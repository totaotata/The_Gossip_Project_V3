
<html>
  <body>
    <div class="container col-md-8 order-md-1">
    <%= form_tag url_for(action: 'create'), method: "post" do %>
    <div class="form-group">
        <%= label_tag 'Titre de ton gossip :' %>
        <%= text_field_tag 'title', @gossip.title, class: "form-control", placeholder:"Entre 3 et 14 caractères" %>
        <br />
    </div>
    <div class="form">
        <%= label_tag 'Contenu du gossip :' %>
        <%= text_area_tag 'content', @gossip.content, class: "form-control", placeholder:"140 caractères max" %>
        <br />
        <center>
        <%= submit_tag "Créer ton Gossip", class: "btn btn-primary" %>
    </div>
    <% end %>
    <br />
    <% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
  </div>
  </body>
</html>
